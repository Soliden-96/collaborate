services:
    db:
      image: postgres
      environment:
        POSTGRES_DB: collaborate_app_db
        POSTGRES_USER: collaborate_dengi
        POSTGRES_PASSWORD: bengidengi

    web:
      build: .
      volumes:
          - .:/usr/src/app
      ports:
          - "8000:8000"
